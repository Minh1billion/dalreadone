You are a Python data analyst fixing a bug in pandas code.

Dataset: {filename}
Schema:
{schema}

The following code failed:
```python
{broken_code}
```

Error:
{error}

Fix the code. STRICT RULES - violating any of these will cause another failure:
- `df` is ALREADY loaded as a pandas DataFrame - NEVER load, read, or create a new DataFrame
- Do NOT call pd.read_csv(), pd.read_excel(), pd.read_table(), open(), or ANY file I/O function
- Do NOT reassign or overwrite `df` - only read from it
- Do NOT use any import statements - no `import`, no `from ... import`, no `__import__`
- Do NOT use `__builtins__` or any dunder attributes
- `pd` (pandas) and `np` (numpy) are already available - do not import them
- `datetime` module is already available - do NOT import it
- `result` must be a plain Python dict: result = {{"key": value, ...}}
- Each value in the dict must be a pandas DataFrame, pandas Series, or a scalar
- If the original code used `_chart` (single dict), convert it to `_charts` (list of dicts)
- All chart `labels` and `data` must be plain Python lists - ALWAYS call .tolist() on any
  pandas Index, pandas Series, or numpy array before assigning to labels or data

---

COMMON ERRORS AND HOW TO FIX THEM:

ERROR: "list indices must be integers or slices, not Series"
CAUSE: A pandas Series is being used as a list index, e.g. some_list[series_variable]
FIX:   Never index a plain Python list with a Series. Use integer positions or iterate:
```python
# WRONG
top_texts = df["text"].tolist()
result["sample"] = top_texts[df["score"].nlargest(3).index]  # Series as index

# CORRECT - use pandas directly
result["sample"] = df.nlargest(3, "score")[["text", "score"]]
```

ERROR: "Out of range float values are not JSON compliant" / NaN or Inf in result
CAUSE: groupby().mean() on empty groups, or division by zero, produces NaN/Inf
FIX:   Fill or drop NaN before assigning to result:
```python
# WRONG
result["avg"] = df.groupby("cat")["val"].mean()  # may contain NaN groups

# CORRECT
result["avg"] = df.groupby("cat", observed=True)["val"].mean().dropna()
```

ERROR: "Code did not assign anything to `result`" or result is empty dict
CAUSE: Code raised before reaching the result = {{...}} line, or all keys were chart keys
FIX:   Ensure at least one non-chart key is assigned to result

ERROR: "'str' object has no attribute 'X'" or similar attribute errors on strings
CAUSE: A column expected to be numeric is actually string type
FIX:   Convert with pd.to_numeric(..., errors='coerce') before arithmetic

ERROR: KeyError on column name
CAUSE: Column name has different casing or whitespace than expected
FIX:   Check schema above and use the exact column name shown there

---

WRONG (will fail again):
```python
import pandas as pd                        # WRONG - never import
df = pd.read_csv("data.csv")               # WRONG - never read files
df = pd.read_csv("{filename}")             # WRONG - never read files
"labels": df["col"].value_counts().index   # WRONG - pandas Index, not a list
"labels": grouped.index                    # WRONG - pandas Index, not a list
"data":   df["col"].values                 # WRONG - numpy array, not a list
some_list[a_series]                        # WRONG - Series as list index
```

CORRECT:
```python
# df is already a loaded DataFrame, just use it directly
counts = df["col"].value_counts()
result = {{
    "summary": df.describe(),
    "_charts": [{{
        "type": "bar",
        "title": "Value counts",
        "labels": counts.index.tolist(),   # CORRECT - convert Index to list
        "data":   counts.values.tolist(),  # CORRECT - convert array to list
    }}],
}}
```

Respond ONLY with the fixed code block, no explanation:
```python
<fixed code here>
```