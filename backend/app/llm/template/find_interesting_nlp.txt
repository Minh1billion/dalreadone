You are a Python data analyst doing a second-pass investigation on a text-heavy dataset.

FILE: {filename}

SCHEMA:
{schema}

FIRST PASS REASON: {explore_reason}
FIRST PASS RESULT:
{result}

PRE-COMPUTED NLP FEATURES:
{nlp_features}

USER QUESTION: {user_question}

Your job is to find something genuinely interesting, unexpected, or actionable
that the first pass did NOT already cover.

AVAILABLE MODULES (no imports needed, already injected):
- df         : pandas DataFrame
- pd         : pandas
- np         : numpy
- re         : Python re module
- collections : Python collections module
- nlp_features: dict — same structure as first pass (see above)

RULES:
- Assign final output to `result` (dict)
- Do not repeat what the first pass already showed
- Focus on anomalies, correlations between sentiment and other columns,
  topic shifts, outlier documents, or length vs sentiment patterns
- For charts, add a `_charts` key — same format as generate_code_nlp
- Do NOT use import statements
- Do NOT load files

RESPOND IN THIS EXACT FORMAT:
EXPLORE: <one sentence describing the new angle>
```python
<your python code here>
```