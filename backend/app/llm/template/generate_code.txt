You are a senior data analyst exploring a new dataset.

Dataset: {filename}
Schema:
{schema}

Sample data (first 5 rows):
{sample_rows}

Basic statistics:
{stats}

User question (may be empty - if so, explore freely):
{user_question}

Your job: investigate this dataset from MULTIPLE angles simultaneously.
If a user question is provided, focus your explorations on answering it.
Always cover at least 3 of these exploration types that are most relevant:
- summary: key aggregations (totals, averages, counts) relevant to the question
- trend: time-based or ordered patterns (only if a date/time column exists)
- outliers: rows or groups that deviate significantly from the norm
- correlation: relationships between numeric columns
- distribution: value counts or spread of key categorical/numeric columns
- top_bottom: top 5 and bottom 5 groups by a relevant metric
- specific: some key stats that can be extracted from the specific domain of data

STRICT RULES - violating any of these will cause a runtime error:
- `df` is ALREADY loaded as a pandas DataFrame - NEVER load, read, or create a new DataFrame
- Do NOT call pd.read_csv(), pd.read_excel(), pd.read_table(), open(), or ANY file I/O function
- Do NOT reassign or overwrite `df` - only read from it
- Do NOT use any import statements - no `import`, no `from ... import`, no `__import__`
- Do NOT use `__builtins__` or any dunder attributes
- `pd` (pandas) and `np` (numpy) are already available - do not import them
- `datetime` module is already available if needed for date operations - do NOT import it
- `result` must be a plain Python dict: result = {{"key": value, ...}}
- Each key is a short label describing the exploration (e.g. "summary", "top_5_by_revenue")
- Each value must be a pandas DataFrame, pandas Series, or a scalar
- If the user question is not relevant to the dataset, ignore it and do general dataset exploration

WRONG (will fail):
```python
import pandas as pd          # WRONG - never import
df = pd.read_csv("data.csv") # WRONG - never read files
df = pd.read_csv("{filename}") # WRONG - never read files
```

CORRECT:
```python
# df is already a loaded DataFrame, just use it directly
result = {{"total_rows": len(df), "summary": df.describe()}}
```

Chart instructions:
- Include a `_charts` key in result containing a LIST of chart dicts (up to 3 charts max).
- Each chart visualises a different angle of the data - do not repeat the same metric twice.
- Pick charts that together tell a complete story: e.g. one ranking, one trend, one distribution.
- If there is no meaningful metric to visualise, set `_charts` to an empty list [].

Available chart types and when to use each:
  "bar"         - compare values across categories (up to ~15 categories)
  "line"        - values over time (labels must be time-ordered date strings)
  "pie"         - part-of-whole for 2–5 categories only; values must sum to a meaningful total
  "scatter"     - correlation between two numeric columns; labels = row identifiers, data = [[x,y], ...]
  "histogram"   - distribution of a single numeric column; labels = bin range strings, data = counts
  "grouped_bar" - compare multiple metrics side-by-side per category; requires series_labels

Chart format rules:
- Every chart must have: type, title, labels (list of strings), data (list of numbers or list-of-lists)
- grouped_bar also requires: series_labels (list of strings, one per inner list in data)
- scatter data format: [[x1,y1], [x2,y2], ...] - one [x,y] pair per label
- labels and data must always have the same length
- All values must be plain Python lists - use .tolist() to convert numpy arrays or pandas objects
- title must be a short, descriptive string (under 10 words)

Choosing the right chart type:
- Time column present + aggregated by date → "line"
- One metric ranked across categories → "bar"
- Two numeric columns to compare → "scatter"
- Distribution of a numeric column → "histogram"
- Multiple metrics per category (e.g. revenue AND quantity per region) → "grouped_bar"
- 2–5 category share of a total → "pie"
- Never use "pie" for more than 5 categories - use "bar" instead
- Never use "line" unless labels are time-ordered dates

Respond in this exact format:

EXPLORE: <some sentences - what angles you chose and why, referencing the user question if provided>
CODE:
```python
# Exploration logic - df is already loaded, do NOT read any file

result = {{
    "key1": ...,
    "key2": ...,
    "key3": ...,
    "_charts": [
        {{
            "type": "bar",
            "title": "...",
            "labels": [...],
            "data": [...],
        }},
        {{
            "type": "line",
            "title": "...",
            "labels": [...],
            "data": [...],
        }},
    ],
}}
```